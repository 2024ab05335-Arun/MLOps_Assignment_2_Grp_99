# Example environment variables for docker-compose and local runs
# Copy to .env and update values before running `docker-compose up`.

# Model URI used by the inference service. Can be an MLflow run URI or file path.
MODEL_URI=runs:/a40b43613cdb458b93c2da1e0a723b02/model

# HTTP port the inference service listens on
PORT=8000

# Docker Hub credentials (optional - only if you want CI to push to Docker Hub)
DOCKERHUB_USERNAME=your-dockerhub-username
DOCKERHUB_PASSWORD=your-dockerhub-password

# (GHCR) No extra secret required for GHCR push if using GITHUB_TOKEN with packages: write
